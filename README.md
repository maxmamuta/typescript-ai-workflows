# typescript-ai-workflows

**Quick start commands:**

- `npm install` - Install node modules
- `npm run dev` - Quick dev start
- `npm run build` - Build JS dist folder
- `npm run start` - Run node.js from dist folder
